# Local app / host tools talking to Docker Postgres on localhost:
DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/payment_system

# Inside docker-compose the app service uses host "db" instead (see docker-compose.yml).
